<section class="tab standard-form{{#if tab.active}} active{{/if}}" data-group="{{tab.group}}" data-tab="{{tab.id}}">
    <fieldset>
        <legend>Coming soon</legend>
        <p class="hint">This medkit isn't built out yet. Shell, state model, and footer are inherited from the base; doc-type tabs land in a follow-up.</p>
    </fieldset>
</section>
